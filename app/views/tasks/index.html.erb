<h3>Task Manager</h3>
<% @tasks.each do |task| %>
  <%= link_to  task_path(task) do %>
    <span>
      <%= task.completed ? "[X]" : [ ] %>
      <%= task.title %>  
    </span> 
    <br>
  <% end %>
<%end  %>